# Build Prompt — v0.4b.1: tighten the tournament "survives a window" rule (fraction gate)

**For the coding session.** Read `CLAUDE.md`, then `same_day_lab/reports/tournament.py`.
Small, surgical fix. Do exactly this.

## The problem

The current `_survives(counts)` in `tournament.py` is `traded_survived_pass_threshold >= 1
and traded_killed_by_friction == 0`. Because a fill-honesty `KILL_STRATEGY` almost never
fires, this collapses to *"had ≥1 friction-surviving day in the window."* On the real
decide/holdout run, **all three strategies were `carried_forward=True` — including
`orb_long_5m`, which survived friction on only 1 of 7 decide days** (a known net loser).
The binary flag contradicts the honest signal in the counts (the in-sample PASS fractions
deflate out-of-sample): it's the over-reading trap leaking back in through a summary.

## The fix (counts-only — NO P&L summation; preserve the no-cumulative-P&L invariant)

Replace the survival rule with a **fraction gate**, using **pre-stated, a-priori
constants** (module-level; do NOT tune them to flip any particular strategy):

```python
MIN_TRADED_DAYS = 3        # too few trades in a window -> can't corroborate
SURVIVE_PASS_FRACTION = 0.50

def _survives(counts) -> bool:
    traded = counts["traded"]
    if traded < MIN_TRADED_DAYS:
        return False
    if counts["traded_killed_by_friction"] > 0:
        return False
    return counts["traded_survived_pass_threshold"] / traded > SURVIVE_PASS_FRACTION
```

- `survives(window)` = at least `MIN_TRADED_DAYS` traded, **a strict majority (>50%) of
  traded days are `PASS_FOR_MORE_TESTING`**, and **0 KILL** days.
- `carried_forward` = survives(decide) **AND** survives(holdout) — unchanged.
- Update the `SURVIVES_RULE` text to state the new rule exactly (the fraction, the floor,
  the a-priori/pre-registered nature).

## Update tests (`tests/test_tournament.py` + the two-window synthetic fixtures)

The synthetic fixtures (2025-08-04..08-12) were built for the OLD rule. Adjust the
fixtures and/or expectations so the gate is **demonstrated** under the new rule — a clear
trichotomy:
- one strategy with a **>50% PASS majority in BOTH** windows → `carried_forward=True`;
- one that clears decide but **fails the holdout** (holdout PASS-fraction ≤50%, or a KILL)
  → `carried_forward=False` (the gate visibly bites);
- one that never reaches a majority → `carried_forward=False`.
Keep the existing assertions that there is **no winner/validation verdict**, the
multiple-comparisons caveat + N are present, and the `tournament_hash` is deterministic
(two runs identical). Full suite stays green.

## Do NOT
- Tune `MIN_TRADED_DAYS` / `SURVIVE_PASS_FRACTION` to produce a specific verdict on the
  real data — they are pre-stated honesty constants, chosen a priori.
- Add P&L summation, a winner verdict, or any portfolio/Sharpe/equity math.
- Touch fill/verdict internals or other strategies. Don't edit `README.md`/`CLAUDE.md` —
  report doc-impacting notes back.

## Acceptance
- `pip install -e ".[test]" && pytest` green, offline.
- `tournament.py` uses the fraction gate; `SURVIVES_RULE` text matches the code.
- On the real run (`tournament --symbol AAPL --decide-start 2025-06-02 --decide-end
  2025-06-13 --holdout-start 2025-06-17 --holdout-end 2025-06-27`), `carried_forward` now
  reflects the majority-in-both rule rather than being trivially all-True (report the
  resulting leaderboard back — do not hardcode an expected count).
- `tournament_hash` still deterministic across two runs.

## Verification
1. `.venv/bin/python -m pytest -v` green.
2. Run the real decide/holdout tournament; confirm `carried_forward` is no longer
   all-True and matches the >50%-PASS-in-both-windows-with-floor rule; run twice → identical
   `tournament_hash`.
